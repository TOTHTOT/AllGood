# All Good 设计系统（DESIGN.md）

> 苹果质感 × 适老化。取 Apple 设计语言的气质——克制用色、大量留白、层次靠色块而非边框、动效细腻；
> 叠加适老化硬规则——大字号、高对比、大触控目标、关键信息永不含糊。
> 本文件是所有 UI 代码的唯一设计依据，Flutter 主题层必须严格映射这里的 token。

## 1. 设计哲学

- **平静优先**：界面是给老年人每天看的，不许制造焦虑。用色克制、信息分层、一次只做一件事。
- **质感来自留白与层次，不来自装饰**：不用渐变背景、不用纹理、不用重阴影。层次靠背景色对比（灰底白卡）和字重区分。
- **一个强调色**：全 App 只有一个品牌强调色（平和蓝），只用于交互元素。健康语义色（正常/警告/异常）是唯一的例外。
- **大即是美**：字号、按钮、间距全部按老年人可轻松阅读和操作的尺度设计，宁可一屏少放一个卡片。

## 2. 色彩 Token

### 基础色（取自 Apple 体系）

| Token | 值 | 用途 |
|-------|-----|------|
| `bgPage` | `#F5F5F7` | 页面背景（Apple 浅灰，比纯白柔和） |
| `bgCard` | `#FFFFFF` | 卡片、输入区背景 |
| `textPrimary` | `#1D1D1F` | 主文本（近黑，非纯黑） |
| `textSecondary` | `#6E6E73` | 次要文本、说明文字 |
| `textTertiary` | `#AEAEB2` | 占位提示（禁止用于关键信息） |
| `divider` | `#E5E5EA` | 分隔线（仅用于列表分组，卡片不用边框） |

### 强调色（唯一品牌色）

| Token | 值 | 用途 |
|-------|-----|------|
| `accent` | `#0A7AFF` | 主按钮、选中态、可点击元素。比 Apple Blue `#0071E3` 略亮，保证在浅灰底上对比度 ≥ 4.5:1 |
| `accentSoft` | `#E5F0FF` | 强调色的浅底（选中标签、进度环底色） |

### 健康语义色（Apple system colors，唯一的多色例外）

| Token | 值 | 用途 |
|-------|-----|------|
| `ok` | `#34C759` | 已打卡、数值正常 |
| `warning` | `#FF9F0A` | 临界、待关注 |
| `danger` | `#FF3B30` | 超阈值、异常提醒（出现时必须附文字说明，不允许只靠颜色传达） |

### 规则

- 除以上 token 外不允许引入任何颜色。
- `danger` 只用于数据本身异常；"请联系长辈确认情况"类提醒用 `warning`，不给家属制造恐慌。
- 深色模式本阶段不实现，但 token 结构要留好位置。

## 3. 字体与字阶

Android 使用系统默认字体（Roboto / Noto Sans CJK），不引入外部字体包。

| 角色 | 字号 (sp) | 字重 | 行高 | 用途 |
|------|----------|------|------|------|
| `display` | 34 | 600 | 1.2 | 页面大标题（"今天感觉怎么样？"） |
| `title` | 28 | 600 | 1.25 | 卡片标题、模块标题 |
| `headline` | 22 | 600 | 1.3 | 打卡项名称、按钮文字 |
| `body` | 20 | 400 | 1.45 | 正文（基准字号，不得小于此值传达关键信息） |
| `bodyStrong` | 20 | 600 | 1.45 | 正文强调 |
| `caption` | 17 | 400 | 1.4 | 辅助说明、时间戳 |
| `reading` | 48 | 600 | 1.1 | 健康读数（血压/血糖数值），数字要一眼看清 |
| `readingUnit` | 17 | 400 | 1.3 | 读数单位（mmHg、mmol/L） |

规则：

- 字重只用 400 和 600 两档，不用 300（太细对老年人不友好），不用 800/900。
- 中文不设置负字距（仅对纯数字读数允许 -0.5）。
- 行高不低于 1.3，正文段落不低于 1.45。

## 4. 间距、圆角、阴影

- **间距**：8pt 网格。常用刻度 8 / 16 / 24 / 32。卡片内边距 20-24，卡片间距 16，页面左右边距 20。
- **圆角**：卡片 20；主按钮全圆角（pill）；输入控件 14；语义标签 8。苹果式大圆角在适老场景放大一档，更亲和。
- **阴影**：全 App 只有一种阴影——`rgba(0,0,0,0.08), offset(0,2), blur 12`，且仅用于悬浮元素（底部弹层、浮动按钮）。普通卡片**无阴影无边框**，靠灰底白卡的色块对比分层。

## 5. 触控目标（硬性适老规则）

- 任何可点击元素 ≥ **56dp**；主操作按钮高度 **64dp**。
- 相邻可点元素间距 ≥ 8dp。
- 禁止：纯图标无文字按钮（图标旁必须有文字标签）；需要精确定位的操作（小步进器、滑块微调）——数值调整用大号 +/- 按钮，步进明显。
- 所有主要操作必须有即时视觉反馈 + 成功结果页（"已记录，今天也要好好吃饭"式正向文案）。

## 6. 核心组件规范

### 打卡卡片（今日页主体）

- 白卡、圆角 20、内边距 24、整卡可点。
- 左侧：大图标（40dp）+ 打卡项名称（`headline`）+ 一句状态说明（`body`，如"8:00 早餐后 1 次"）。
- 右侧：未完成 → 大号"打卡"按钮（accent 填充，pill，高 56）；已完成 → `ok` 色对勾图标 + "已打卡"文字。
- 异常数据的卡片：卡片顶部出现 `danger` 色细条 + 文字说明，不整卡变红。

### 数据录入（底部弹层 BottomSheet）

- 从底部升起，圆角 28（顶部），高度约屏 75%。
- 数值输入优先用**大滚轮或大 +/- 步进器**，避免唤起小键盘；血压录入选"高压/低压"两个大数字字段。
- 底部固定一个高 64 的 accent 主按钮（"完成记录"）。

### 趋势图表（家属端）

- 极简柱状/折线：无网格线、无 Y 轴刻度数字堆叠，只有正常范围色带（`ok` 10% 透明度）+ 数据点。
- 正常范围内数据点用 accent，超出范围用 danger 并加大点。
- 每张图下配一句人话总结（`body`），如"最近 7 天血压平稳"。

### 提醒与反馈

- App 内提醒用顶部柔和横幅（`warning` 浅底 + 深色文字），不用突兀的红色弹窗。
- 打卡成功：整屏或半屏正向反馈，大对勾 + 一句温暖文案，2 秒后自动消失或点按关闭。

## 7. 文案语气

- 对老年用户：温暖、简短、像家人说话。"今天吃药了吗？"而不是"请完成用药打卡任务"。
- 对家属：客观、不渲染恐慌。"妈妈今天还没打卡，方便时联系一下她"，而不是"警告：用户长时间未响应！"
- 按钮文案用动词短语，≤ 6 个字："打卡"、"已吃药"、"记录血压"。

## 8. Flutter 落地映射

| 设计 token | Flutter 落点 |
|-----------|-------------|
| 色彩 | `ThemeData.colorScheme` + `AppColors` 常量类 |
| 字阶 | `TextTheme`（display/title/headline/body/caption 自定义映射） |
| 圆角/间距/触控 | `AppDimens` 常量类 + 组件主题（`cardTheme`、`elevatedButtonTheme`、`bottomSheetTheme`） |
| 组件 | `CheckInCard`、`ReadingSheet`、`TrendCard`、`GentleBanner` 等基础组件，页面只允许组合这些组件 |

页面开发规则：**不允许在页面里散落裸的颜色值和字号**，一律从 token 取。
